# Plan

The plan command will output all resources that need to be created / destroyed by the BuildFlow project in the current directory.

Run `buildflow plan --help` to see available options:

```
$ buildflow plan --help
Usage: buildflow plan [OPTIONS] APP

  Output all resources used by a buildflow flow or grid

Options:
  --help          Show this message and exit.

```