The plan command will create / update all resources that are used by the BuildFlow project in the current directory.

Run `buildflow apply --help` to see available options:

```
$ buildflow apply --help
Usage: buildflow apply [OPTIONS]

  Apply all resources used by a buildflow flow or grid

Options:
  --help          Show this message and exit.
```