# Destroy

The destroy command will delete all resources that are used by the BuildFlow application in the current directory.

Run `buildflow destroy --help` to see available options:


```
$ buildflow destroy --help
Usage: buildflow destroy [OPTIONS]

  Destroy all resources used by a buildflow application

Options:
  --help          Show this message and exit.
```