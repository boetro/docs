# Destroy

The destroy command will delete all resources that are used by the BuildFlow project in the current directory.

Run `buildflow destroy --help` to see available options:


```bash
$ buildflow destroy --help
Usage: buildflow destroy [OPTIONS]

  Destroy all resources used by a buildflow flow or grid

Options:
  --help          Show this message and exit.
```