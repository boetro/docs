The preview command will output all resources that need to be created / destroyed by the BuildFlow application in the current directory.

Run `buildflow preview --help` to see available options:

```
$ buildflow preview --help
Usage: buildflow preview [OPTIONS] APP

  Output all resources used by a buildflow application

Options:
  --help          Show this message and exit.

```