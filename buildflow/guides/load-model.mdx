---
title: "Load a Remote Model"
---

In this guide we will go over how to load model that is stored in a remote bucket (GCS or S3). We will load the model at server startup and ensure that it gets used across requets.

