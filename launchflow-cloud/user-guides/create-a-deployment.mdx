---
title: Create a Deployment
---

Creating a deployment is as simple as clicking the `Launch Deployment` button in the VSCode extension, or by using the `deployment submit` command in the CLI.

TODO update link to CLI.

<Tabs>
  <Tab title="VS Code Extension">
  ![Launch](/images/launch.gif)
  </Tab>
  <Tab title="CLI">
    Use the [deployment submit](TODO) command from the root directory of your project to create a new deployment.

    ```
        launch deployments submit
    ```
  </Tab>
</Tabs>